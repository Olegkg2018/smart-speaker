dependencies:
  # 5.2 нужен ради driver/i2c_master.h — на нём работает экран.
  idf: ">=5.2"
  espressif/esp_websocket_client: "^1.2.0"
  # Адресный светодиод WS2812 на плате — обычным GPIO им не поуправляешь.
  espressif/led_strip: "^2.5.5"
  # Звуковой фронтенд Espressif: эхоподавление (микрофон перестаёт слышать
  # собственный динамик) плюс автоусиление и шумодав — без них колонка
  # выполняла команды из играющей песни и слышала только вплотную.
  espressif/esp-sr: "^2.5.1"
